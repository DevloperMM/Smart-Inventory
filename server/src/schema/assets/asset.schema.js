/**
 * @swagger
 * components:
 *   schemas:
 *     Asset:
 *       type: object
 *       properties:
 *         id:
 *           type: integer
 *         category:
 *           type: string
 *         mfgBy:
 *           type: string
 *         modelNo:
 *           type: string
 *         description:
 *           type: string
 *         serialNo:
 *           type: integer
 *         materialCode:
 *           type: string
 *         pr:
 *           type: string
 *         po:
 *           type: string
 *         srr:
 *           type: string
 *         grn:
 *           type: string
 *         stockedBy:
 *           type: integer
 *         updatedBy:
 *           type: integer
 *         inWarranty:
 *           type: boolean
 *         startDate:
 *           type: date
 *         endDate:
 *           type: date
 *         amcVendor:
 *           type: string
 *         storeId:
 *           type: integer
 *         addInfo:
 *           type: string
 *         status:
 *           type: string
 *       required:
 *         - category
 *         - mfgBy
 *         - modelNo
 *         - description
 *         - serialNo
 *         - stockedBy
 *         - inWarranty
 *         - startDate
 *         - endDate
 *         - storeId
 *         - status
 */
